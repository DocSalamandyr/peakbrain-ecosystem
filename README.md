# @peakbrain/ecosystem

The one membership client for the Peak Brain sites: the pricing catalog, the entitlement resolver, the neurofeedback.space cookie settings, and a client for the nogn.app registry's entitlement API. No secrets live here; every site supplies its own service token through its environment.

Public so the sites can fetch it at build time. Owned by Peak Brain; all rights reserved.
